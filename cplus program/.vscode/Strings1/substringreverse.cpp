#include<iostream>
using namespace std;
int main(){
    // string s = "Dileep";
    // cout<<s<<" ";
    // int n = s.length();
    // // s.substr(idx,len)
    // cout<<s.substr(n/2)<<" ";
    int x = 123456;
    string s = to_string(x);
    cout<<s.length();
}